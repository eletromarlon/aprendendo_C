#include <stdio.h>
int main (){
    int x,y,z;
    printf("Tipologia dos triângulos!\n");
    scanf("%d", &x);
    scanf("%d", &y);
    scanf("%d", &z);

    if ((x==y) && (y==z))
    if ((x==y) && (y==z))
    if ((x==y) && (y==z))
   }


